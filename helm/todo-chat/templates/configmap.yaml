apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "todo-chat.fullname" . }}-config
  labels:
    {{- include "todo-chat.labels" . | nindent 4 }}
data:
  # Backend API URL for frontend
  backend-url: {{ .Values.frontend.env.NEXT_PUBLIC_API_URL | quote }}
